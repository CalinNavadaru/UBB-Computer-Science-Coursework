package com.example.myfirstapp.core.data.remoteimport com.google.gson.GsonBuilderimport okhttp3.OkHttpClientimport retrofit2.Retrofitimport retrofit2.converter.gson.GsonConverterFactoryobject Api {    // date_join    //private val url = "172.20.10.3:3000"    //acasa    private val url = "192.168.81.72:8000"//    private val url = "192.168.1.133:8000"    private val httpUrl = "http://$url/"    val wsUrl = "ws:/$url/ws/employees/"    private var gson = GsonBuilder().create()    val tokenInterceptor = TokenInterceptor()    val okHttpClient = OkHttpClient.Builder().apply {        this.addInterceptor(tokenInterceptor)    }.build()    val retrofit = Retrofit.Builder()        .baseUrl(httpUrl)        .addConverterFactory(GsonConverterFactory.create(gson))//        .client(okHttpClient) // does not work in android with the latest libs        .build()}